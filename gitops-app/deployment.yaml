apiVersion: apps/v1
kind: Deployment
metadata:
  name: gitops-app
  namespace: default
spec:
  replicas: 1
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      k8s-app: gitops-app
  template:
    metadata:
      labels:
        k8s-app: gitops-app
    spec:
      containers:
        - name: gitops-app
          image: ghcr.io/kirqe/gitops-app:2311f4830718bdcd558ed81526d642ab1fc8a441
          imagePullPolicy: Always
          ports:
            - containerPort: 8080