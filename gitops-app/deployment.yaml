apiVersion: apps/v1
kind: Deployment
metadata:
  name: gitops-app
  namespace: default
spec:
  replicas: 1
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      k8s-app: gitops-app
  template:
    metadata:
      labels:
        k8s-app: gitops-app
    spec:
      containers:
        - name: gitops-app
          image: ghcr.io/kirqe/gitops-app:6f1fbfe2a11b06f5347fa041cea0a5138e6c44cf
          imagePullPolicy: Always
          ports:
            - containerPort: 8080